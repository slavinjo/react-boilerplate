import React from 'react';
import './Footer.css';

const footer = (props) => {
    return (
        <footer className="footer">
            Footer...
        </footer>
    );
}

export default footer;